services:

  llm-model:
    image: llm-model
    build:
      context: gguf_model
      dockerfile: Dockerfile
    ports:
      - 8007:8007
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
